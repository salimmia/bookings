{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                {{$res := index .UserInformation "user_information"}}
                <h1>My Name is: {{$res.FirstName}} {{$res.LastName}}</h1>
            </div>
        </div>
    </div>
{{end}}